# ─── CORE (API Nash) ─────────────────────────────────────────────
flask>=2.3
gunicorn>=21.0.0
openai>=1.13
pinecone>=3.0
python-dotenv>=1.0
requests>=2.32
tiktoken>=0.7              # opcional, bom para controle de tokens

# ─── AUTONOMIA & SCHEDULING ──────────────────────────────────────
apscheduler>=3.10          # cron interno (self‑review, jobs periódicos)

# ─── GIT & GITHUB INTEGRAÇÃO ────────────────────────────────────
GitPython>=3.1             # manipular repositório localmente
PyGithub>=1.59             # acesso à API GitHub (PRs, issues, commits)

# ─── (REMOVA O RESTANTE POR ENQUANTO) ───────────────────────────
# beautifulsoup4, pandas, fastapi, etc. ficam para quando realmente for ativar scraping,
# análise local, micro‑services, etc. Mantemos o mínimo viável e já preparamos o solo
# para Nash evoluir em automações e deploys GitHub‑driven!